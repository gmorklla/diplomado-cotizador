### 🔍 Explicación del Diagrama

El diagrama representa la arquitectura del **Sistema de Cotización de Hardware**. Se divide en 4 bloques lógicos:

1.  **El Núcleo (Jerarquía de Artículos):**
    *   **`Articulo` (Clase Padre):** Es la base de todo. Contiene los datos generales (`marca`, `modelo`, `precio`). Tiene el método `crearArticulo` (Patrón **Factory Method**) para instanciar hijos sin usar `new`.
    *   **`TipoArticulo` (Enum):** Define qué tipos existen (Monitor, Disco, etc.) para ayudar al Factory.
    *   **Hijos Concretos:** `Monitor`, `TarjetaVideo`, `DiscoDuro` y `Pc` heredan de `Articulo`.

2.  **El Desacoplamiento (Interfaz `IComponentePc`):**
    *   Es el contrato que dice "yo puedo ir adentro de una PC".
    *   **¿Quién lo implementa?** `Monitor`, `TarjetaVideo` y `DiscoDuro`.
    *   **¿Quién NO lo implementa?** La `Pc`. Esto evita que metas una PC dentro de otra PC.
    *   **Método `getArticulo()`:** Es el puente para obtener los datos del padre (`Articulo`) desde la interfaz.

3.  **La Estructura Compuesta (`Pc`):**
    *   La clase `Pc` hereda de `Articulo` (por eso se puede cotizar), pero tiene una relación de **agregación** (el rombo blanco) con `IComponentePc`. Esto significa que la PC "tiene" muchos componentes dentro.

4.  **La Construcción Compleja (Patrón `PcBuilder`):**
    *   `PcBuilder` es una clase auxiliar encargada de armar la PC paso a paso.
    *   Tiene las reglas de negocio (`MAX_DISCOS = 4`, etc.).
    *   La `Pc` tiene una dependencia hacia el Builder (`getBuilder()`), y el Builder devuelve una `Pc` (`build()`).

---

### 🛠️ Código para Mermaid

Copia y pega el siguiente bloque de código en cualquier editor de Mermaid (como [Mermaid Live Editor](https://mermaid.live/) o en Notion/Obsidian/GitHub).

```mermaid
classDiagram
    %% Relaciones de Dependencia (Flechas punteadas)
    TestCotizador ..> Cotizador
    TestCotizador ..> Articulo
    TestCotizador ..> Pc

    %% Relaciones de Herencia (Flechas sólidas con triángulo)
    Articulo <|-- Monitor
    Articulo <|-- TarjetaVideo
    Articulo <|-- DiscoDuro
    Articulo <|-- Pc

    %% Relaciones de Implementación (Flechas punteadas con triángulo)
    IComponentePc <|.. Monitor
    IComponentePc <|.. TarjetaVideo
    IComponentePc <|.. DiscoDuro

    %% Relaciones de Agregación (Rombos)
    Cotizador o-- "1..*" Articulo : IstArticulos
    Pc o-- "1..*" IComponentePc : IstComponentes

    %% Dependencia del Builder
    Pc ..> PcBuilder : usa
    PcBuilder ..> Pc : crea

    %% Enum
    class TipoArticulo {
        <<enumeration>>
        TARJETA_VIDEO
        MONITOR
        DISCO_DURO
        PC
    }

    %% Clases Principales
    class TestCotizador {
        +main() void
        +testCotizador() void
    }

    class Cotizador {
        -lstArticulos: List<Articulo>
        -lstCantidades: List<Integer>
        +cotizar() void
        +agregarItemCotizacion(Articulo, Integer) void
    }

    class Articulo {
        -marca: String
        -modelo: String
        -precioBase: BigDecimal
        -costo: BigDecimal
        -sku: String
        +getters() void
        +setters() void
        +cotizar(Integer) BigDecimal
        +getTipo() TipoArticulo
        +crearArticulo(Map~String,Object~) Articulo
    }

    class Monitor {
        +cotizar(Integer) BigDecimal
    }

    class TarjetaVideo {
        -memoria: String
        -PROMO_LLEVE_N_XM_VALOR_N: int {readOnly}
        -PROMO_LLEVE_N_XM_VALOR_M: int {readOnly}
        +cotizar(Integer) BigDecimal
    }

    class DiscoDuro {
        -capacidadAlmacenamiento: String
        +cotizar(Integer) BigDecimal
    }

    class IComponentePc {
        <<interface>>
        +getArticulo() Articulo
    }

    class Pc {
        -lstComponentes: List<IComponentePc>
        +cotizar(Integer) BigDecimal
        +agregarComponente(IComponentePc) void
        +getBuilder() PcBuilder
    }

    class PcBuilder {
        -MAX_DISCOS: int = 4 {readOnly}
        -MAX_MONITORES: int = 2 {readOnly}
        -MAX_TARJETAS: int = 1 {readOnly}
        -lstDiscos: List<DiscoDuro>
        -lstMonitores: List<Monitor>
        -tarjeta: TarjetaVideo
        -parametros: Map~String,Object~
        +agregarParametro(String, Object) PcBuilder
        +agregarDiscoDuro(IComponentePc) PcBuilder
        +agregarMonitor(IComponentePc) PcBuilder
        +agregarTarjetaVideo(IComponentePc) PcBuilder
        +build() Pc
        -configuracionValida() boolean
        +validarConfiguracion() Map~String,String~
    }

    %% Notas
    note for PcBuilder "Configurar correctamente un objeto de estructura compleja (la pc)"
    
    %% Relación Enum
    Articulo ..> TipoArticulo
```

### 💡 Detalles Técnicos de la Traducción

1.  **Tipos Genéricos:** En Mermaid, los caracteres `<` y `>` a veces dan problemas si no se escapan bien. He usado la tilde `~` (ej. `Map~String,Object~`) que es la forma estándar en Mermaid para representar genéricos visualmente.
2.  **Visibilidad:**
    *   `+` significa `public`.
    *   `-` significa `private`.
3.  **Relaciones:**
    *   `<|--` es Herencia (extends).
    *   `<|..` es Realización (implements).
    *   `o--` es Agregación (tiene un...).
    *   `..>` es Dependencia (usa un...).
4.  **ReadOnly:** Las propiedades constantes (como `MAX_DISCOS`) las marqué con `{readOnly}` al final, tal como aparecen en herramientas de modelado UML.